//Write a function that determines if a string is a palindrome,
//ignoring capitalisation and non alphabetical characters.
//
//Should return 0 if NOT a palindrome.

/*-----------------------------------------*/
//YOUR CODE HERE

int isPalindrome( char * string ){

}

/*-----------------------------------------*/

int main(){

    char * str1 = "abcdedcba";
    char * str2 = "AbCdEeDcBa"
    char * str3 = "Are we not drawn onward, we few, drawn onward to new era?";
    char * srt4 = "CATSCATS";

    assert( isPalimdrome( str1 ) );
    assert( isPalimdrome( str2 ) );
    assert( isPalimdrome( str3 ) );
    assert( !isPalimdrome( str4 ) );

    printf("All tests passedi :)\n");


}
